# ============================================
# PRODUÇÃO - CONFIGURAÇÃO SSL/TLS COM NGINX
# ============================================
# Este arquivo documenta como fazer deploy em produção com HTTPS

# ✅ Domínio configurado: api.barstock.coderonin.com.br
# 
# Se precisar mudar o certificado, execute:
#   bash init-letsencrypt.sh api.barstock.coderonin.com.br seu-email@coderonin.com.br
#
# Para deploy:
#   docker-compose -f docker-compose.prod.yml up -d

# ============================================
# VARIÁVEIS DE AMBIENTE BACKEND
# ============================================
NODE_ENV=production

# Banco de dados
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/estoque
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=estoque
DB_PORT=5432

# JWT
JWT_SECRET=sua-chave-secreta-super-segura-aqui-mude-isso

# Porta (interna, nginx redireciona por fora)
PORT=3000

# ============================================
# NOTAS
# ============================================
# 1. O nginx roda na porta 80 (HTTP) e 443 (HTTPS)
# 2. O backend roda internamente na porta 3000
# 3. Certbot renova automaticamente a cada 12 horas
# 4. Sempre use HTTPS em produção
# 5. Mantenha JWT_SECRET seguro e aleatório
